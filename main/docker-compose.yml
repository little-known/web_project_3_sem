version: "3.8"

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    working_dir: /app
    environment:
      NODE_ENV: production
    volumes:
      - sqlite_data:/app/data
    ports:
      - "3000:3000"
    restart: unless-stopped

volumes:
  sqlite_data:
